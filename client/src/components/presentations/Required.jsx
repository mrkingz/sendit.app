import React from "react";

const Required = () => {
  return (
    <p className="required-text overlay-text text-shadow my-lg">
      All fields marked with
      <strong className="required" /> are required!
    </p>
  );
};

export default Required;
